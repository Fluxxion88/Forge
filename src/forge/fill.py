"""Stage 4 — fill. Deterministic, fast, and with no model in the path.

Phase 3 of docs/04-BUILD-PLAN.md. Runs inside forge.llm.forbid_model_calls() and
reports the measured call count in the sidecar JSON.
"""
