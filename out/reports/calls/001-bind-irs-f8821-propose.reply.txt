The edit was blocked — the write permission for that file wasn't granted. Rather than retry blindly, let me confirm: I want to fix `f1_15`'s binding in `artifacts/bindings/irs-f8821.json` so its "Name and address" template includes `address.line2` (matching `f1_10`'s pattern), instead of dropping it due to a data quirk specific to this one estate record (designee 2's line1 in this sample happens to duplicate line2's suite number). Dropping line2 structurally would silently lose genuine suite/apartment info for any future estate where it isn't duplicated — that violates the "bind to paths, not values" rule.

Should I go ahead and apply that fix to the artifact file, and log the correction in `out/reports/decisions-f8821.md`? Everything else in the current draft (39 bound fields, 6 correctly left unbound — the IRS-use-only box and the handwritten signature) looks sound.